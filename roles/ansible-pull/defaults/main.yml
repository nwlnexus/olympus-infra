---
ansible_pull_state: present
ansible_pull_repo: https://github.com/nwlnexus/olympus-infra.git
ansible_pull_interval: 1h

# Schedule for the daily run. Default: 02:00 local. Override per-host if
# different timing is needed (e.g., to stagger across multiple hosts).
# 03:00 is reserved for unattended-upgrades auto-reboot — don't collide.
ansible_pull_hour: 2
ansible_pull_minute: 0

# Service identity that owns the cron job, log dir, and pull workspace.
# Changed from "{{ ansible_user }}" (deployer) to a dedicated service user
# so the cron survives deployer-account changes.
ansible_pull_user: ansible

# Old cron owners to clean up by the one-shot migration task.
# Default empty; populate via host_vars when migrating an existing host.
ansible_pull_legacy_users: []

# Set per-host in inventory group_vars
ansible_pull_inventory: ""   # e.g. inventory/management.yml
ansible_pull_playbook: ""    # e.g. playbooks/pull/management.yml

# Optional --limit flag for ansible-pull. When set, restricts playbook to this
# host/pattern. For compute-hub nodes, set to "$(hostname -s)" so host_vars
# and group_vars are loaded correctly in pull mode.
# Leave empty for single-host inventories (management-hub, ai-hub).
ansible_pull_limit: ""

# macOS: path to ansible-pull binary installed via Homebrew
ansible_pull_bin_macos: /opt/homebrew/bin/ansible-pull
# Linux: usually on PATH for the ansible user
ansible_pull_bin_linux: /usr/bin/ansible-pull

ansible_pull_log_dir: /var/log/ansible-pull
ansible_pull_log_file: "{{ ansible_pull_log_dir }}/run.log"
