---
- name: Render data-services role (check-mode only)
  hosts: all
  gather_facts: false
  vars:
    ansible_os_family: Darwin
  tasks:
    - name: Include data-services role
      ansible.builtin.include_role:
        name: data-services
      vars:
        _op_env: {}
