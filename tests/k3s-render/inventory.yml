---
all:
  hosts:
    fake-agent:
      ansible_connection: local
    fake-server:
      ansible_connection: local
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_become: false
    _op_env: {}
